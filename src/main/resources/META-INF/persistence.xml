<persistence xmlns="http://java.sun.com/xml/ns/persistence" version="2.0">
    <persistence-unit name="student_pu" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>com.annie.model.Student</class>
        <class>com.annie.model.School</class>

        <properties>
            <!-- Sử dụng file-based H2 database -->
            <property name="javax.persistence.jdbc.url" value="jdbc:h2:file:./data/test;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE"/>
            <property name="javax.persistence.jdbc.driver" value="org.h2.Driver"/>
            <property name="javax.persistence.jdbc.user" value="sa"/>
            <property name="javax.persistence.jdbc.password" value=""/>

            <!-- Hibernate settings -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/> <!-- Không tạo lại bảng mỗi lần chạy -->
        </properties>
    </persistence-unit>
</persistence>
